import { SampleSearchPipe } from './sample-search.pipe';

describe('SampleSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SampleSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
